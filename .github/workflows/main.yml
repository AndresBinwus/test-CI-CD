name: Manual Required Check

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for running this manual check'
        required: true
        default: 'Manual trigger'

jobs:
  manual_check:
    runs-on: ubuntu-latest
    steps:
      - name: Perform Manual Check
        run: |
          echo "Running manual checks..."
          # Add your manual check logic here
          # For example, a script that performs a specific verification
          # If the check fails, exit with a non-zero code to indicate failure
          # exit 1 
          echo "Manual check completed successfully."